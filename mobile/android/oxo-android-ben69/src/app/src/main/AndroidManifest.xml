<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="CloudShare Pro"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        
        <!-- Main Activities -->
        <activity
            android:name="co.ostorlab.cloudshare.MainActivity"
            android:exported="true"
            android:label="CloudShare Pro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name="co.ostorlab.cloudshare.FilesActivity"
            android:exported="false"
            android:label="My Files" />
            
        <activity
            android:name="co.ostorlab.cloudshare.ShareActivity"
            android:exported="false"
            android:label="Share Files" />
            
        <activity
            android:name="co.ostorlab.cloudshare.AccountActivity"
            android:exported="false"
            android:label="Account Settings" />
            
        <activity
            android:name="co.ostorlab.cloudshare.SecurityActivity"
            android:exported="false"
            android:label="Security &amp; Privacy" />
            
        <activity
            android:name="co.ostorlab.cloudshare.UploadActivity"
            android:exported="false"
            android:label="Upload Files" />

        <!-- Content Provider - VULNERABLE but looks legitimate -->
        <provider
            android:name="co.ostorlab.cloudshare.providers.ShareContentProvider"
            android:authorities="co.ostorlab.cloudshare.shares"
            android:exported="true"
            android:grantUriPermissions="true" />
            
    </application>

</manifest>